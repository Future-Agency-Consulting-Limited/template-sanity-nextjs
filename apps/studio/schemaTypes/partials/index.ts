/**
 * Sanity Schema Types - Partials Index
 *
 * Use this file to export all partial schema types so they're available in
 * Sanity studio.
 */
import { copyType } from "@/schemaTypes/partials/copyType";
import { pageBuilderType } from "@/schemaTypes/partials/pageBuilderType";

export const partialSchemaTypes = [copyType, pageBuilderType];
