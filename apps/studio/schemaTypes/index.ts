import { objects } from "@/schemaTypes/objects";
import { partials } from "@/schemaTypes/partials";
import { documents } from "@/schemaTypes/documents";

export const schemaTypes = [...objects, ...partials, ...documents];
